name: "Changelog Check"

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  main:
    name: Ensure changelog has been updated
    runs-on: ubuntu-latest
    steps:
      - name: Verify readme files
        uses: dorny/paths-filter@v2.2.1
        id: filter
        with:
          filters: |
            theme_update:
              - '**/**'
            excluded_subfolders:
              - 'variations/**'
              - '.github/**'
              - '.husky/**'
              - '.phab/**'
            readme:
              - '**/readme.txt'
      - name: Reminder to update changelog
        if: ${{ steps.filter.outputs.theme_update == 'true' && steps.filter.outputs.excluded_folders == 'false' && steps.filter.outputs.readme == 'false' }}
        uses: actions/github-script@v3
        with:
          script: |
              core.setFailed('Please update the changelog in the readme.txt of any updated themes.')